table! {
    users (id) {
        id -> Int4,
        username -> Varchar,
        email -> Varchar,
        firstname -> Varchar,
        lastname -> Varchar,
        password -> Varchar,
        role -> Varchar,
    }
}
